{% extends 'base.html.twig' %}


{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-body">
                You confirmed your email address you can <a href="{{path('login')}}">login</a> now.
            </div>
        </div>
    </div>
{% endblock %}
